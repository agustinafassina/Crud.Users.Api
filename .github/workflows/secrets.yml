name: Secrets read

on:
  push:
    branches:
      - main

jobs:
  secrets-read:
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v4
    - name: Checkout code
      run: echo ${{ secrets.AWS_REGION }}